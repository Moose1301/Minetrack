version: '3'

services:
  minetrack:
    build: .
    container_name: minetrack
    dns:
      - 8.8.8.8
      - 1.1.1.1
    ports:
      - "${MINETRACK_PORT}:8080"
    restart: always
    volumes:
      - ${MINETRACK_DATA}/database.sql:/usr/src/minetrack/database.sql
